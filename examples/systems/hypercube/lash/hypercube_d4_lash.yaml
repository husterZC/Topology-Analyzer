name: hypercube_d4_lash

topology:
  type: hypercube
  params:
    dimension: 4

links:
  default:
    latency_cycles: 1
    bandwidth: 64GB/s

routing:
  type: hypercube_lash
  max_vcs: 4
  candidate_paths: 8
  seed: 0
